const root=document.documentElement;const docPath=root.dataset.doc;function esc(s){return s.replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))}function md(src){let out='',inCode=false,code=[];const lines=src.replace(/\r/g,'').split('\n');for(const raw of lines){if(raw.startsWith('```')){if(inCode){out+=`<pre><code>${esc(code.join('\n'))}</code></pre>`;code=[];inCode=false}else inCode=true;continue}if(inCode){code.push(raw);continue}if(!raw.trim()){out+='';continue}if(/^### /.test(raw))out+=`<h3>${inline(raw.slice(4))}</h3>`;else if(/^## /.test(raw))out+=`<h2>${inline(raw.slice(3))}</h2>`;else if(/^# /.test(raw))out+=`<h1>${inline(raw.slice(2))}</h1>`;else if(/^> /.test(raw))out+=`<blockquote>${inline(raw.slice(2))}</blockquote>`;else if(/^[-*] /.test(raw))out+=`<ul><li>${inline(raw.slice(2))}</li></ul>`;else if(/^\d+\. /.test(raw))out+=`<ol><li>${inline(raw.replace(/^\d+\. /,''))}</li></ol>`;else if(/^---+$/.test(raw.trim()))out+='<hr>';else out+=`<p>${inline(raw)}</p>`}return out.replace(/<\/ul><ul>/g,'').replace(/<\/ol><ol>/g,'')}function inline(s){return esc(s).replace(/\*\*(.+?)\*\*/g,'<strong>$1</strong>').replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\[([^\]]+)\]\(([^)]+)\)/g,'<a href="$2">$1</a>')}async function load(){if(!docPath)return;const target=document.querySelector('[data-doc-content]');try{const r=await fetch(docPath);if(!r.ok)throw new Error(r.status);const text=await r.text();target.innerHTML=md(text)}catch(e){target.innerHTML='<h1>文件載入失敗</h1><p>請確認此頁面與 Markdown 文件的相對路徑。</p>'}}load();